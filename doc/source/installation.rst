============
Installation
============

At the command line::

    $ pip install fuel-tanker

Or, if you have virtualenvwrapper installed::

    $ mkvirtualenv fuel-tanker
    $ pip install fuel-tanker
